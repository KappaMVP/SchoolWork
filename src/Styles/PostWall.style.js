import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  wall: {
    flex: 1,
    width: '100%',
    flexDirection: 'row',
    flexWrap: 'wrap',
    alignContent: 'flex-start',
  },
});
